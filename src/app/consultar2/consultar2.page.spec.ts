import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Consultar2Page } from './consultar2.page';

describe('Consultar2Page', () => {
  let component: Consultar2Page;
  let fixture: ComponentFixture<Consultar2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Consultar2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
